1. 编写一个C++程序,将句子中的单词位置倒置（单词以空格分割），而不改变单词内部结构，例如将“I am from Shanghai”倒置为“Shanghai from am I”。

2. 编写C++函数int strToInt()，将字符串s转化为整型数返回。例如，输入字符串参数“12345”，该函数返回整数12345；输入字符串参数“-123”，该函数返回整数-123。
注意负数处理方法，使用符号位来处理负数。
函数原型：
int strToInt(const std::string s);

3. 编写C++函数int strToInt()，将k进制(2<=k<=10)的字符串s转化为整型数返回。例如，输入字符串参数“111”和进制参数2，该函数返回整数7；输入字符串参数“-11”和进制参数3，该函数返回整数-4。
注意负数处理方法，使用符号位来处理负数。
函数原型：
int strToInt(const std::string s, int k);



请在附件中的.cpp和.h文件的//TODO后添加代码，使程序完整。test1_output.txt为test1_input.txt为测试输入时的输出。请同学们使用这些测试数据来验证自己的程序。

请把所有的.cpp和.h文件打包提交。